# Publish SDD 95-5 Wiki Update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the new SDD 95-5 concept and four related `llm_wiki` updates as validated Chinese website content.

**Architecture:** Use the existing snapshot-based publication pipeline: sync the source inventory, merge source changes into curated Chinese pages, then finalize atomically to update the public index and manifest. The existing validators enforce the privacy boundary, link integrity, Chinese content, deterministic indexing, and Wiki QA safety.

**Tech Stack:** Node.js, Markdown, VitePress, repository `wiki:sync` / `wiki:finalize` / `wiki:validate` scripts.

---

### Task 1: Stage and verify the source inventory

**Files:**
- Generate (gitignored): the publication change report and verified source snapshots.

- [ ] **Step 1: Install dependencies in the isolated worktree**

Run: `npm install`

Expected: dependencies install successfully without changing tracked package metadata.

- [ ] **Step 2: Sync the public source snapshot**

Run:

```bash
npm run wiki:sync -- --wiki "$LLM_WIKI_PATH"
```

Expected: `Wiki sync: 1 added, 4 changed, 38 unchanged, 0 deleted`.

- [ ] **Step 3: Inspect the generated change report**

Verify that the report lists only `concepts/sdd-95-5-principle.md` as added; `concepts/agent-task-briefing.md`, `concepts/ai-coding-engineering-loop.md`, `concepts/sdd-layered-adoption-model.md`, and `concepts/spec-driven-development.md` as changed; and no deleted page.

### Task 2: Curate the five Chinese public pages

**Files:**
- Create in public concept directory: `sdd-95-5-principle.md`
- Modify in public concept directory: `spec-driven-development.md`
- Modify in public concept directory: `sdd-layered-adoption-model.md`
- Modify in public concept directory: `agent-task-briefing.md`
- Modify in public concept directory: `ai-coding-engineering-loop.md`

- [ ] **Step 1: Create the new Chinese concept page**

Create `sdd-95-5-principle.md` in the public concept directory with public frontmatter fields only:

```yaml
---
title: "SDD 95-5 原则"
type: "concept"
tags: ["ai-coding", "workflow", "planning", "context-engineering", "evaluation"]
created: "2026-07-05"
updated: "2026-07-05"
---
```

Translate the complete source into Chinese under these exact headings:

```markdown
# SDD 95-5 原则
## 最小可用规格
## 采用顺序
## 工具选择问题
## 失效模式
## 相关内容
```

Preserve the source's meaning that 95/5 is an adoption heuristic rather than a measured statistic. Convert every related wikilink to its canonical public concept route.

- [ ] **Step 2: Merge the new source material into the four existing translations**

Apply only the new 2026-07-05 material shown in the verified source snapshots:

- `spec-driven-development.md`: set `updated` to `2026-07-05`; add the habit-before-tooling adoption paragraph after the layered adoption paragraph; add `SDD 95-5 原则` to related content.
- `sdd-layered-adoption-model.md`: set `updated` to `2026-07-05`; add the minimal handwritten-spec threshold after the proportional adoption bullets; add `SDD 95-5 原则` to related content.
- `agent-task-briefing.md`: set `updated` to `2026-07-05`; add the four-heading smallest-useful-spec paragraph after the alignment paragraph; add `SDD 95-5 原则` to related content.
- `ai-coding-engineering-loop.md`: set `updated` to `2026-07-05`; add the manual-spec-before-tooling principle to the SDD lifecycle paragraph; add `SDD 95-5 原则` to related content.

Do not replace unchanged Chinese prose or copy private `sources` metadata.

- [ ] **Step 3: Run pre-finalization boundary checks**

Run:

```bash
rg -n 'sources:|raw/|\[\[[^]]+\]\]' docs/wiki
```

Expected: no matches and exit 1.

### Task 3: Finalize, validate, and commit the publication

**Files:**
- Modify: the generated public wiki index.
- Modify: `wiki-manifest.json`
- Modify: the generated Wiki QA search index artifact.
- Include the five page files from Task 2.

- [ ] **Step 1: Finalize the atomic publication**

Run: `npm run wiki:finalize`

Expected: `43 published pages`.

- [ ] **Step 2: Verify the new page is indexed**

Run:

```bash
rg -n 'sdd-95-5-principle|SDD 95-5 原则' wiki-manifest.json docs/wiki
```

Expected: matches in the public page, generated index, and manifest.

- [ ] **Step 3: Run the complete verification suite**

Run: `npm test`

Expected: all publication tests, Worker tests, wiki validation, content checks, theme checks, VitePress production build, and security scan pass.

- [ ] **Step 4: Confirm no unexpected generated changes**

Run: `git status --short`

Expected tracked changes: the five concept pages, generated public wiki index, `wiki-manifest.json`, generated Wiki QA search index artifact, and this task's design/plan documents only.

- [ ] **Step 5: Commit the publication**

```bash
git add -u
git add docs/wiki
git commit -m "content: publish SDD 95-5 wiki update"
```
